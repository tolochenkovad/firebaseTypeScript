import styled from "styled-components";

const LoginStyles = styled.div`
  .box {
    padding-top: 50px;
  }

  .title {
    text-align: center;
    margin-bottom: 50px;
  }
`;

export default LoginStyles;
